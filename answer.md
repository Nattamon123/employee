1🔴 ไม่มี ADR เรื่อง Auth — ตอนนี้เราบอกว่า "ไม่ใช้ Supabase Auth" แล้วก็จบ ไม่มีที่ไหนบอกว่า JWT จะ Issue ยังไง Login Flow เป็นยังไง ถ้าไม่มี App Developer จะเขียนหน้า Login ไม่ได้
ตอบ = ผมควรใช้ supabase auth หรือใช้ firebaseดี
รีวิว = 
🔴 ไม่มี Database Schema — เรารู้ว่ามี User, Attendance, OffsiteRequest แต่ไม่มีใครบอกว่าตาราง Supabase หน้าตาเป็นยังไง คอลัมน์อะไรบ้าง ถ้าไม่มี Go จะเขียน SQL ยิงไปไหนไม่รู้
ตอบ = ใน @employee script ผมได้เอา script sheet เก่า พร้อม schema ข้อมูลเก่าไว้แล้ว
รีวิว =

admin ใช้ stackอะไร 
ตอบ admin ใช้ vite แล้วใช้หลังบ้าน go เหมือนกันดีไหม
รีวิว